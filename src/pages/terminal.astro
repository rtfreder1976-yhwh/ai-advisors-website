---
// src/pages/terminal.astro
// Standalone Funnel Page for Voice Search Dominance
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Voice Search Dominance | The Service Access Terminal</title>
        <meta
            name="description"
            content="Dominate voice search in your city. Stop being invisible to Siri and Alexa."
        />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;800;900&display=swap"
            rel="stylesheet"
        />
        <style>
            :root {
                --primary: #4facfe;
                --secondary: #00f2fe;
                --accent: #10b981;
                --bg-dark: #0f172a;
                --bg-darker: #020617;
                --text-main: #f8fafc;
                --text-muted: #94a3b8;
                --glass: rgba(255, 255, 255, 0.03);
                --glass-border: rgba(255, 255, 255, 0.1);
                --font-sans: "Inter", system-ui, -apple-system, sans-serif;
            }

            * {
                box-sizing: border-box;
                margin: 0;
                padding: 0;
            }

            body {
                font-family: var(--font-sans);
                background-color: var(--bg-darker);
                color: var(--text-main);
                line-height: 1.6;
                overflow-x: hidden;
            }

            /* Typography */
            h1,
            h2,
            h3 {
                font-weight: 800;
                letter-spacing: -0.025em;
            }

            .gradient-text {
                background: linear-gradient(
                    90deg,
                    var(--primary),
                    var(--secondary)
                );
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
            }

            /* Layout Containers */
            .container {
                max-width: 1100px;
                margin: 0 auto;
                padding: 0 2rem;
            }

            header {
                padding: 2rem 0;
                text-align: center;
            }

            .logo {
                font-size: 1.5rem;
                font-weight: 900;
                text-transform: uppercase;
                letter-spacing: 0.1em;
                color: var(--primary);
            }

            /* Hero Section */
            .hero {
                padding: 4rem 0 8rem;
                text-align: center;
            }

            .hero h1 {
                font-size: clamp(2.5rem, 5vw, 4rem);
                margin-bottom: 1.5rem;
                line-height: 1.1;
            }

            .hero p {
                font-size: 1.25rem;
                color: var(--text-muted);
                max-width: 700px;
                margin: 0 auto 3rem;
            }

            /* Premium Video Terminal */
            .vsl-container {
                position: relative;
                max-width: 800px;
                margin: 0 auto;
                padding: 10px;
                background: var(--glass);
                border: 1px solid var(--glass-border);
                border-radius: 20px;
                box-shadow: 0 50px 100px -20px rgba(0, 0, 0, 0.7);
            }

            .vsl-placeholder {
                width: 100%;
                aspect-ratio: 16/9;
                background: #000;
                border-radius: 12px;
                display: flex;
                align-items: center;
                justify-content: center;
                overflow: hidden;
            }

            /* Sections */
            section {
                padding: 8rem 0;
            }

            .section-head {
                text-align: center;
                margin-bottom: 4rem;
            }

            .section-head h2 {
                font-size: 2.5rem;
                margin-bottom: 1rem;
            }

            /* Pain Cards */
            .pain-grid {
                display: grid;
                grid-template-columns: 1fr 1fr;
                gap: 4rem;
                align-items: center;
            }

            .before-after-img {
                width: 100%;
                border-radius: 20px;
                border: 1px solid var(--glass-border);
            }

            /* Feature Grid */
            .feature-grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
                gap: 2rem;
            }

            .card {
                padding: 2.5rem;
                background: var(--glass);
                border: 1px solid var(--glass-border);
                border-radius: 24px;
                transition: all 0.3s ease;
            }

            .card:hover {
                background: rgba(255, 255, 255, 0.06);
                border-color: var(--primary);
                transform: translateY(-5px);
            }

            .card-icon {
                width: 50px;
                height: 50px;
                margin-bottom: 1.5rem;
            }

            /* Pricing Table */
            .pricing {
                display: grid;
                grid-template-columns: repeat(3, 1fr);
                gap: 1.5rem;
                margin-top: 4rem;
            }

            .price-tier {
                padding: 3rem 2rem;
                background: var(--glass);
                border: 1px solid var(--glass-border);
                border-radius: 24px;
                display: flex;
                flex-direction: column;
            }

            .price-tier.featured {
                border: 2px solid var(--primary);
                transform: scale(1.05);
                background: rgba(79, 172, 254, 0.05);
                position: relative;
            }

            .price-tier.featured::before {
                content: "MOST POPULAR";
                position: absolute;
                top: -15px;
                left: 50%;
                transform: translateX(-50%);
                background: var(--primary);
                padding: 4px 16px;
                border-radius: 20px;
                font-size: 0.75rem;
                font-weight: 800;
            }

            .price-tier h3 {
                font-size: 1.5rem;
                margin-bottom: 1rem;
            }

            .price-tier .cost {
                font-size: 3rem;
                font-weight: 900;
                margin-bottom: 2rem;
            }

            .price-list {
                list-style: none;
                margin-bottom: 2rem;
                flex-grow: 1;
            }

            .price-list li {
                margin-bottom: 1rem;
                color: var(--text-muted);
                display: flex;
                align-items: center;
                gap: 10px;
            }

            .price-list li::before {
                content: "✓";
                color: var(--accent);
                font-weight: bold;
            }

            /* Buttons */
            .btn {
                display: inline-block;
                padding: 1rem 2.5rem;
                border-radius: 12px;
                font-weight: 800;
                text-decoration: none;
                text-transform: uppercase;
                letter-spacing: 0.05em;
                cursor: pointer;
                transition: all 0.3s ease;
                text-align: center;
                border: none;
            }

            .btn-primary {
                background: linear-gradient(
                    90deg,
                    var(--primary),
                    var(--secondary)
                );
                color: #000;
            }

            .btn-primary:hover {
                box-shadow: 0 0 30px rgba(79, 172, 254, 0.4);
            }

            .btn-outline {
                background: transparent;
                border: 1px solid var(--glass-border);
                color: var(--text-main);
            }

            .btn-outline:hover {
                border-color: var(--primary);
            }

            /* Footer / Sticky CTA */
            .sticky-footer {
                position: fixed;
                bottom: 0;
                left: 0;
                width: 100%;
                padding: 1.5rem 0;
                background: rgba(2, 6, 23, 0.9);
                backdrop-filter: blur(20px);
                border-top: 1px solid var(--glass-border);
                transform: translateY(100%);
                transition: transform 0.5s ease;
                z-index: 1000;
            }

            .sticky-footer.visible {
                transform: translateY(0);
            }

            .sticky-content {
                display: flex;
                justify-content: space-between;
                align-items: center;
            }

            /* Animations */
            @keyframes fadeIn {
                from {
                    opacity: 0;
                    transform: translateY(20px);
                }
                to {
                    opacity: 1;
                    transform: translateY(0);
                }
            }

            .animate {
                animation: fadeIn 1s forwards;
            }

            /* Mobile Responsive */
            @media (max-width: 900px) {
                .pricing {
                    grid-template-columns: 1fr;
                }
                .pain-grid {
                    grid-template-columns: 1fr;
                }
                .hero h1 {
                    font-size: 3rem;
                }
                .price-tier.featured {
                    transform: scale(1);
                }
                .stack-grid {
                    grid-template-columns: 1fr !important;
                }
            }
        </style>
    </head>
    <body>
        <header>
            <div class="container">
                <div class="logo">
                    VOICE<span class="gradient-text">DOMINANCE</span>
                </div>
            </div>
        </header>

        <main>
            <!-- HERO SECTION -->
            <section class="hero container animate">
                <h1 class="">
                    How to Dominate Voice Search in Your City <span
                        class="gradient-text">Without Lifting a Finger.</span>
                </h1>
                <p>
                    Stop being invisible to Siri, Alexa, and Google Assistant.
                    We fix the data fragmentation that's killing your local lead
                    flow—guaranteed.
                </p>

                <div class="vsl-container">
                    <div class="vsl-placeholder">
                        <div
                            style="text-align:center; color: var(--text-muted)"
                        >
                            <div style="font-size: 3rem; margin-bottom: 1rem">
                                ▶
                            </div>
                            <p>[VSL: THE VOICE REVOLUTION]</p>
                        </div>
                    </div>
                </div>

                <div style="margin-top: 4rem">
                    <a href="#pricing" class="btn btn-primary"
                        >YES, SECURE MY RANKINGS</a
                    >
                    <p
                        style="font-size: 0.8rem; margin-top: 1rem; color: var(--text-muted)"
                    >
                        9 Spots Left for February forensic audits.
                    </p>
                </div>
            </section>

            <!-- PAIN SECTION -->
            <section style="background: var(--bg-dark)">
                <div class="container">
                    <div class="pain-grid">
                        <div>
                            <h2
                                style="font-size: 2.5rem; margin-bottom: 1.5rem"
                            >
                                Your Business is Currently <span
                                    style="color: #ef4444">Invisible</span
                                > to 50% of Your Market.
                            </h2>
                            <p style="margin-bottom: 1.5rem">
                                Every time a potential customer asks Siri "Find
                                a local business near me," a decision is made in
                                milliseconds. If your listing isn't manually
                                verified, you don't exist.
                            </p>
                            <p>
                                <strong>The Suspension Threat:</strong> Inconsistent
                                data triggers Google's manual review bots. One wrong
                                digit on a random directory can lead to a "Digital
                                Death Sentence."
                            </p>
                        </div>
                        <div>
                            <img
                                src="/images/funnel/voice_search_before_after.png"
                                alt="Before and After Voice Search"
                                class="before-after-img"
                            />
                        </div>
                    </div>
                </div>
            </section>

            <!-- MECHANISM SECTION -->
            <section class="container">
                <div class="section-head">
                    <h2>
                        The <span class="gradient-text"
                            >Synchronization Protocol</span>
                    </h2>
                    <p style="max-width: 600px; margin: 0 auto">
                        We don't use software "overlays" that hide the problem.
                        We use Forensic Metadata Cleanup to lock your data at
                        the source.
                    </p>
                </div>

                <div class="feature-grid">
                    <div class="card">
                        <img
                            src="/images/funnel/suspension_shield.png"
                            alt="Suspension Shield"
                            class="card-icon"
                        />
                        <h3>Suspension Shield</h3>
                        <p>
                            We monitor your Google Business Profile 24/7 to
                            block 'Suggest an Edit' attacks from competitors.
                        </p>
                    </div>
                    <div class="card">
                        <div class="card-icon" style="font-size: 2.5rem">
                            🍎
                        </div>
                        <h3>Manual Apple Sync</h3>
                        <p>
                            Direct API submission and manual phone verification
                            to ensure Siri priority.
                        </p>
                    </div>
                    <div class="card">
                        <div class="card-icon" style="font-size: 2.5rem">
                            🏰
                        </div>
                        <h3>Citation Fortress</h3>
                        <p>
                            50+ high-authority niche citations to cement your
                            NAP data across the entire web ecosystem.
                        </p>
                    </div>
                </div>
            </section>

            <!-- OFFER STACK SECTION -->
            <section style="background: var(--bg-dark)">
                <div
                    class="container stack-grid"
                    style="display: grid; grid-template-columns: 1fr 1fr; gap: 4rem; align-items: center"
                >
                    <div>
                        <img
                            src="/images/funnel/value_stack.png"
                            alt="Offer Value Stack"
                            style="width: 100%; border-radius: 20px"
                        />
                    </div>
                    <div>
                        <h2 style="margin-bottom: 2rem">
                            The <span class="gradient-text">Grand Slam</span> Offer.
                        </h2>
                        <ul class="price-list" style="font-size: 1.1rem">
                            <li>
                                Voice Search Protocol (Core) - <span
                                    style="color: var(--text-main)"
                                    >$1,997 Value</span>
                            </li>
                            <li>
                                Suspension Shield Audit - <span
                                    style="color: var(--text-main)"
                                    >$497 Value</span>
                            </li>
                            <li>
                                Siri & Alexa Native Integration - <span
                                    style="color: var(--text-main)"
                                    >$997 Value</span>
                            </li>
                            <li>
                                Review Gating Strategy Session - <span
                                    style="color: var(--text-main)"
                                    >$497 Value</span>
                            </li>
                            <li>
                                Total High-Authority Citation Kit - <span
                                    style="color: var(--text-main)"
                                    >$512 Value</span>
                            </li>
                        </ul>
                        <hr
                            style="border: none; border-top: 1px solid var(--glass-border); margin: 2rem 0"
                        />
                        <p style="font-size: 1.25rem; margin-bottom: 2rem">
                            Total Value: <span
                                style="text-decoration: line-through; color: #ef4444"
                                >$4,500+</span>
                        </p>
                        <div
                            style="font-size: 3rem; font-weight: 900"
                            id="pricing"
                        >
                            ONLY $997
                        </div>
                        <p
                            style="color: var(--text-muted); margin-bottom: 2rem"
                        >
                            (One-Time Forensic Setup Fee)
                        </p>
                        <button class="btn btn-primary" id="stack-cta"
                            >CLAIM MY ACCESS TERMINAL</button
                        >
                    </div>
                </div>
            </section>

            <!-- PRICING TABLE -->
            <section class="container">
                <div class="section-head">
                    <h2>
                        Choose Your Level of <span class="gradient-text"
                            >Dominance</span>
                    </h2>
                </div>
                <div class="pricing">
                    <div class="price-tier">
                        <h3>DIY Checklist</h3>
                        <div class="cost">$0</div>
                        <ul class="price-list">
                            <li>PDF Guidebook</li>
                            <li>List of 50 Directories</li>
                            <li>Basic SEO Tips</li>
                        </ul>
                        <button class="btn btn-outline">DOWNLOAD PDF</button>
                    </div>
                    <div class="price-tier featured">
                        <h3>Grand Slam</h3>
                        <div class="cost">$997</div>
                        <ul class="price-list">
                            <li>Full Forensic Audit</li>
                            <li>Manual Apple/Bing sync</li>
                            <li>Suspension Shield</li>
                            <li>50+ Citations Build</li>
                            <li>Review Gating Strategy</li>
                        </ul>
                        <button class="btn btn-primary checkout-trigger"
                            >GET STARTED NOW</button
                        >
                    </div>
                    <div class="price-tier">
                        <h3>Enterprise</h3>
                        <div class="cost">$2,997</div>
                        <ul class="price-list">
                            <li>Everything in Grand Slam</li>
                            <li>Monthly Content Creation</li>
                            <li>Reputation Management</li>
                            <li>Dedicated Account Manager</li>
                        </ul>
                        <button class="btn btn-outline checkout-trigger"
                            >GO ENTERPRISE</button
                        >
                    </div>
                </div>
            </section>

            <!-- GUARANTEE SECTION -->
            <section
                style="text-align: center; border-top: 1px solid var(--glass-border)"
            >
                <div class="container">
                    <div style="font-size: 3rem; margin-bottom: 1rem">🛡️</div>
                    <h2>The "Waste-No-Time" Guarantee</h2>
                    <p
                        style="max-width: 700px; margin: 2rem auto; font-size: 1.25rem"
                    >
                        We guarantee we will verify your Apple Maps listing and
                        sync your NAP data to the top 4 aggregators within 30
                        days. <strong
                            >If we fail, we refund 100% of your investment +
                            $100 for wasting your time.</strong
                        >
                    </p>
                </div>
            </section>
        </main>

        <!-- STICKY FOOTER -->
        <div class="sticky-footer" id="sticky-cta">
            <div class="container sticky-content">
                <div>
                    <span style="font-weight: 800"
                        >VOICE DOMINANCE PROTOCOL</span
                    > - <span class="gradient-text">$997</span>
                </div>
                <button
                    class="btn btn-primary checkout-trigger"
                    style="padding: 0.75rem 1.5rem; font-size: 0.8rem"
                    >GET STARTED</button
                >
            </div>
        </div>

        <!-- MODAL -->
        <div
            id="checkout-modal"
            style="display:none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.9); z-index: 2000; align-items: center; justify-content: center;"
        >
            <div
                class="card"
                style="max-width: 500px; width: 90%; background: var(--bg-dark); border: 1px solid var(--glass-border); padding: 3rem; border-radius: 24px"
            >
                <h2 style="margin-bottom: 1rem">Secure Checkout</h2>
                <p style="margin-bottom: 2rem; color: var(--text-muted)">
                    Connect your business to the protocol.
                </p>
                <form
                    id="funnel-form"
                    style="display: flex; flex-direction: column; gap: 1rem"
                >
                    <input
                        type="text"
                        placeholder="Full Name"
                        style="padding: 1rem; border-radius: 8px; border: 1px solid var(--glass-border); background: #000; color: #fff"
                        required
                    />
                    <input
                        type="email"
                        placeholder="Email Address"
                        style="padding: 1rem; border-radius: 8px; border: 1px solid var(--glass-border); background: #000; color: #fff"
                        required
                    />
                    <input
                        type="tel"
                        placeholder="Phone Number"
                        style="padding: 1rem; border-radius: 8px; border: 1px solid var(--glass-border); background: #000; color: #fff"
                        required
                    />
                    <button
                        type="submit"
                        class="btn btn-primary"
                        style="margin-top: 1rem">CONTINUE TO PAYMENT -></button
                    >
                    <button
                        type="button"
                        id="close-modal"
                        style="background: transparent; color: var(--text-muted); border: none; cursor: pointer; margin-top: 1rem"
                        >Cancel</button
                    >
                </form>
            </div>
        </div>

        <script is:inline>
            const footer = document.getElementById("sticky-cta");
            const modal = document.getElementById("checkout-modal");
            const closeBtn = document.getElementById("close-modal");
            const triggers = document.querySelectorAll(
                ".checkout-trigger, #stack-cta",
            );

            window.onscroll = function () {
                if (window.scrollY > 800) {
                    footer.classList.add("visible");
                } else {
                    footer.classList.remove("visible");
                }
            };

            triggers.forEach((t) =>
                t.addEventListener("click", () => {
                    modal.style.display = "flex";
                }),
            );

            closeBtn.addEventListener("click", () => {
                modal.style.display = "none";
            });

            document
                .getElementById("funnel-form")
                .addEventListener("submit", (e) => {
                    e.preventDefault();
                    // Redirect to a thank you page or payment
                    window.location.href = "/thank-you";
                });
        </script>
    </body>
</html>
